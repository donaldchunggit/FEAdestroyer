# =========================
# FEAdestroyer (Windows + Py3.11 + CUDA)
# NOTE:
# Install CUDA PyTorch separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Install PyG + compiled ops separately (match your torch version):
#   pip install torch-geometric
#   pip install torch-scatter torch-sparse torch-cluster torch-spline-conv -f https://data.pyg.org/whl/torch-<TORCH_VER>+cu121.html
# =========================

# Core utilities / CLI
tqdm>=4.66.0
rich>=13.7.0
pyyaml>=6.0.1
psutil>=5.9.8

# Scientific computing
numpy>=1.26.4
scipy>=1.11.4
pandas>=2.2.0
scikit-learn>=1.4.0

# Visualization & data
matplotlib>=3.8.2
plotly>=5.18.0
seaborn>=0.13.2
pyvista>=0.43.0

# Experiment tracking (optional)
wandb>=0.16.3

# Optional quality-of-life (safe)
ipython>=8.20.0
